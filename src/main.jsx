import React from "react";
import ReactDOM from "react-dom/client";
import CNUNavigator from "./CNUNavigator.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CNUNavigator />
  </React.StrictMode>
);
